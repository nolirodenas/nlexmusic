// placeholder for global state (Redux/Zustand/etc.)
// Export a simple React context as a minimal starting point
import React from 'react';
export const AppContext = React.createContext({ user: null });
export default AppContext;
